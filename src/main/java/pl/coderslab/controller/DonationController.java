package pl.coderslab.controller;

import org.springframework.web.bind.annotation.RestController;

/**Spring 4.0 introduced the @RestController annotation in order to simplify the creation of RESTful web services.
 * combines @Controller and @ResponseBody, which eliminates the need to annotate every request handling method of the controller class
 * with the @ResponseBody annotation.
 */

@RestController
public class DonationController {



}
